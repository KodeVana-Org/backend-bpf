import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const VissionScreen = () => {
  return (
    <View>
      <Text>VissionScreen</Text>
    </View>
  );
};

export default VissionScreen;

const styles = StyleSheet.create({});
